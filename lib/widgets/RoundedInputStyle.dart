import 'package:flutter/material.dart';

const roundedInputStyle = const OutlineInputBorder(
    borderRadius: BorderRadius.all(Radius.circular(15)),
    borderSide: BorderSide(color: Colors.white));
